kind = "web"
previewPath = "/"
title = "Handy Controller"
version = "1.0.0"
id = "artifacts/handy-controller"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20441

[services.development]
run = "pnpm --filter @workspace/handy-controller run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/handy-controller", "run", "build" ]
publicDir = "artifacts/handy-controller/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20441"
BASE_PATH = "/"
