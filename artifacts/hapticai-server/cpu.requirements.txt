# CPU-only PyTorch — works on any Windows PC without an NVIDIA GPU.
# Processing is slower than GPU but runs on all hardware.
# Force-reinstalled over the CUDA wheels from the previous build step.
--index-url https://download.pytorch.org/whl/cpu
torch
torchvision
